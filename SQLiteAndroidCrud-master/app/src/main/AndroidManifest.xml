<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.rhildred.crud">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SelectPlayer1Activity" />
        <activity android:name=".SelectPlayer2Activity" />
        <activity android:name=".gameEmulatorActivity" />
        <activity android:name=".ScoreBoardActivity" />
        <activity android:name=".AddPlayerActivity"></activity>
    </application>

</manifest>